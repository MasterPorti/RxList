"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";
type Summary={patients:any[];nurses:any[];floors:any[]};
export default function ManagementPage(){
 const router=useRouter(),[summary,setSummary]=useState<Summary|null>(null),[notice,setNotice]=useState(""),[share,setShare]=useState<any>(null);
 const [nurse,setNurse]=useState({name:"",birthDate:"",floor:"",phone:""}),[patient,setPatient]=useState({fullName:"",birthDate:"",floor:"",bed:"",reason:""}),[med,setMed]=useState({patientId:"",name:"",dose:"",times:"",startDate:new Date().toISOString().slice(0,10),nurseId:"",notes:""});
 async function load(){const r=await fetch("/api/doctor/summary");if(!r.ok){router.push("/login");return}setSummary(await r.json())}useEffect(()=>{load()},[]);
 async function post(url:string,body:any,onOk:(x:any)=>void){setNotice("");const r=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const x=await r.json();if(!r.ok){setNotice("No se pudo guardar: "+(x.error||"revisa los datos"));return}onOk(x);await load()}
 function createNurse(e:React.FormEvent){e.preventDefault();post("/api/nurses",{...nurse,floor:nurse.floor?Number(nurse.floor):undefined},x=>{setShare(x);setNurse({name:"",birthDate:"",floor:"",phone:""})})}
 function createPatient(e:React.FormEvent){e.preventDefault();post("/api/patients",{...patient,floor:patient.floor?Number(patient.floor):undefined,bed:patient.bed?Number(patient.bed):undefined},()=>{setNotice("Paciente agregado correctamente.");setPatient({fullName:"",birthDate:"",floor:"",bed:"",reason:""})})}
 function createMedication(e:React.FormEvent){e.preventDefault();const times=med.times.split(",").map(x=>x.trim()).filter(Boolean);post("/api/medications",{...med,times,nurseId:med.nurseId||undefined},()=>{setNotice("Orden y tareas del medicamento creadas correctamente.");setMed({...med,patientId:"",name:"",dose:"",times:"",nurseId:"",notes:""})})}
 if(!summary)return <main className="page">Cargando…</main>;
 return <main className="shell"><header className="topbar"><div className="brand"><span className="mark">✚</span> RXList</div><div className="topright"><span>Gestión manual</span><button className="btn ghost" onClick={()=>router.push("/doctor")}>Volver al panel</button></div></header><section className="page"><div className="eyebrow">Operación manual</div><h1 className="headline">Altas y órdenes</h1><p className="sub">Puedes realizar estas acciones sin usar el chatbot. Los medicamentos se registran exactamente como los indiques.</p>{notice&&<p className="error">{notice}</p>}<div className="dashboard manualgrid"><section className="panel"><div className="panelhead"><span className="paneltitle">Agregar enfermera</span><span className="count">Acceso temporal</span></div><form className="manualform" onSubmit={createNurse}><label className="formlabel">Nombre completo<input className="field" value={nurse.name} onChange={e=>setNurse({...nurse,name:e.target.value})} placeholder="Juanito Enrique Sánchez" required/></label><label className="formlabel">Fecha de nacimiento<input className="field" type="date" value={nurse.birthDate} onChange={e=>setNurse({...nurse,birthDate:e.target.value})} required/></label><label className="formlabel">Piso<select className="field" value={nurse.floor} onChange={e=>setNurse({...nurse,floor:e.target.value})}><option value="">Sin asignar</option>{summary.floors.map(f=><option key={f.id} value={f.id}>Piso {f.id} · {f.name}</option>)}</select></label><label className="formlabel">Teléfono opcional<input className="field" value={nurse.phone} onChange={e=>setNurse({...nurse,phone:e.target.value})}/></label><button className="btn primary">Crear enfermera y acceso</button></form></section><section className="panel"><div className="panelhead"><span className="paneltitle">Agregar paciente</span><span className="count">Datos básicos</span></div><form className="manualform" onSubmit={createPatient}><label className="formlabel">Nombre completo<input className="field" value={patient.fullName} onChange={e=>setPatient({...patient,fullName:e.target.value})} required/></label><label className="formlabel">Fecha de nacimiento<input className="field" type="date" value={patient.birthDate} onChange={e=>setPatient({...patient,birthDate:e.target.value})} required/></label><label className="formlabel">Piso<select className="field" value={patient.floor} onChange={e=>setPatient({...patient,floor:e.target.value})}><option value="">Sin asignar</option>{summary.floors.map(f=><option key={f.id} value={f.id}>Piso {f.id} · {f.name}</option>)}</select></label><label className="formlabel">Cama opcional<input className="field" type="number" min="1" value={patient.bed} onChange={e=>setPatient({...patient,bed:e.target.value})}/></label><label className="formlabel">Motivo de ingreso<input className="field" value={patient.reason} onChange={e=>setPatient({...patient,reason:e.target.value})}/></label><button className="btn primary">Guardar paciente</button></form></section></div><section className="panel manualmed"><div className="panelhead"><span className="paneltitle">Agregar medicamento o cuidado</span><span className="count">Requiere paciente ingresado</span></div><form className="manualform medform" onSubmit={createMedication}><label className="formlabel">Paciente<select className="field" value={med.patientId} onChange={e=>setMed({...med,patientId:e.target.value})} required><option value="">Selecciona un paciente</option>{summary.patients.filter(p=>p.status!=="discharged"&&typeof p.floor==="number").map(p=><option key={p.id} value={p.id}>{p.fullName} · Piso {p.floor}</option>)}</select></label><label className="formlabel">Medicamento o cuidado<input className="field" value={med.name} onChange={e=>setMed({...med,name:e.target.value})} placeholder="Nombre indicado por el doctor" required/></label><label className="formlabel">Dosis indicada<input className="field" value={med.dose} onChange={e=>setMed({...med,dose:e.target.value})} placeholder="Ej. 2 tabletas"/></label><label className="formlabel">Horarios<input className="field" value={med.times} onChange={e=>setMed({...med,times:e.target.value})} placeholder="07:00, 20:00" required/><small className="fieldhint">Usa horarios explícitos separados por comas.</small></label><label className="formlabel">Fecha de inicio<input className="field" type="date" value={med.startDate} onChange={e=>setMed({...med,startDate:e.target.value})} required/></label><label className="formlabel">Enfermera responsable<select className="field" value={med.nurseId} onChange={e=>setMed({...med,nurseId:e.target.value})}><option value="">Asignar después</option>{summary.nurses.filter(n=>n.status!=="inactive").map(n=><option key={n.id} value={n.id}>{n.name} · Piso {n.floor}</option>)}</select></label><label className="formlabel fullfield">Observaciones<textarea className="field" value={med.notes} onChange={e=>setMed({...med,notes:e.target.value})}/></label><button className="btn primary">Crear orden y tareas</button></form></section><section className="panel"><div className="panelhead"><span className="paneltitle">Pacientes actuales</span><span className="count">{summary.patients.length}</span></div><div className="tablewrap"><table className="table"><thead><tr><th>Paciente</th><th>Fecha nacimiento</th><th>Piso</th><th>Estado</th></tr></thead><tbody>{summary.patients.map(p=><tr key={p.id}><td><strong>{p.fullName}</strong></td><td>{p.birthDate}</td><td>{typeof p.floor==="number"?"Piso "+p.floor:"Sin asignar"}{p.bed?" · Cama "+p.bed:""}</td><td>{p.status}</td></tr>)}</tbody></table></div></section></section></main>}
