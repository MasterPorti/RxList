"use client";
import { useState } from "react";import { useRouter } from "next/navigation";
export default function Login(){const [email,setEmail]=useState("admin@rxlist.local"),[password,setPassword]=useState("RXList-Admin-2026!"),[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(!r.ok){setError("Correo o contraseña incorrectos.");return}router.push("/")}return <main className="loginpage"><section className="loginintro"><div className="brand"><span className="mark">✚</span> RXList</div><div><div className="eyebrow" style={{color:"#a6ded6"}}>Control clínico, con calma</div><h1>El turno bajo control.</h1><p>Una vista clara para que los equipos médicos puedan concentrarse en lo importante: cuidar.</p></div><small style={{color:"#a6ded6"}}>MVP local · acceso protegido</small></section><section className="loginform"><div className="eyebrow">Bienvenido de nuevo</div><h2>Inicia sesión</h2><p className="sub">Accede a tu espacio de trabajo RXList.</p><form onSubmit={submit}><label className="formlabel">Correo electrónico<input className="field" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="formlabel">Contraseña<input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="btn primary" style={{width:"100%",marginTop:24,padding:13}}>Entrar al panel →</button></form><p style={{fontSize:12,color:"var(--muted)",marginTop:24}}>Demo local: admin@rxlist.local</p></section></main>}
