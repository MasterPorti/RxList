"use client";
import {useEffect,useState} from "react";
export default function NurseAccessModal(){const [access,setAccess]=useState<any[]>([]);useEffect(()=>{const handler=(e:Event)=>setAccess((e as CustomEvent).detail||[]);window.addEventListener("rxlist:nurse-access",handler);return()=>window.removeEventListener("rxlist:nurse-access",handler)},[]);if(!access.length)return null;const n=access[0];const message=n.message||`RXList\nUsuario: ${n.email}\nContraseña temporal: ${n.password}\nEntra en /login y cambia tu contraseña.`;return <div className="modalback"><section className="proposal accessmodal"><div className="proposalhead"><div><div className="eyebrow">Cuenta creada</div><strong style={{fontFamily:"Manrope",fontSize:18}}>Acceso de {n.name}</strong></div><button className="iconbtn" onClick={()=>setAccess([])}>×</button></div><div className="proposalbody"><div className="credential"><span>Correo</span><strong>{n.email}</strong></div><div className="credential"><span>Contraseña temporal</span><strong>{n.password}</strong></div><label className="formlabel">Mensaje para compartir<textarea className="field" readOnly value={message}/></label><p className="fieldhint">Comparte estos datos manualmente y solicita el cambio de contraseña en el primer acceso.</p></div><div className="proposalfoot"><button className="btn" onClick={()=>navigator.clipboard.writeText(message)}>Copiar mensaje</button><button className="btn primary" onClick={()=>setAccess([])}>Cerrar</button></div></section></div>}
