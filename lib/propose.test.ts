import {describe,expect,it} from "vitest";import {propose} from "./propose";import type {Doctor} from "./types";
const d:Doctor={id:"d",name:"D",email:"d@x",passwordHash:"x",role:"doctor",nurses:[{id:"s",name:"Sofía Rivero",floor:1},{id:"p",name:"Pablo Martínez",alias:"Pablito",floor:2},{id:"j",name:"Juan Pérez",alias:"Juanito",floor:3}]};
describe("propuestas acotadas",()=>{it("resuelve alias",()=>expect(propose("mueve a Pablito al piso 3",d).operations[0].nurseId).toBe("p"));it("permite lote",()=>expect(propose("mueve a Sofía al piso 2 y a Pablito al piso 3",d).operations).toHaveLength(2));it("rechaza operaciones peligrosas",()=>expect(propose("elimina doctores",d).type).toBe("rejected"));it("pregunta por piso",()=>expect(propose("mueve a Sofía",d).type).toBe("clarification"));it("detecta no-op",()=>expect(propose("mueve a Pablito al piso 2",d).type).toBe("no_change"));it("registra una enfermera cuando el segundo turno aporta el piso",()=>{const first="agrega a Julio Morales";const second=`<HISTORIAL>\nDoctor: ${first}\nAsistente: ¿En qué piso deseas registrar a Julio Morales?\n</HISTORIAL>\n<ULTIMO_MENSAJE_DEL_DOCTOR>\nen el piso 3\n</ULTIMO_MENSAJE_DEL_DOCTOR>`;expect(propose(`${first}\n${second}`,d)).toMatchObject({type:"proposal",operations:[{action:"create_nurse",name:"Julio Morales",floor:3,to:3}]});});});
