import { z } from "zod";
export const Floor = z.union([z.literal(1),z.literal(2),z.literal(3),z.literal(4),z.literal("unassigned")]);
export const AssignedFloor = z.union([z.literal(1),z.literal(2),z.literal(3),z.literal(4)]);
export type FloorId = 1|2|3|4;
export type Nurse = { id:string; name:string; birthDate?:string; alias?:string; email?:string; phone?:string; floor:FloorId|"unassigned"; status?:"active"|"inactive"; userId?:string; shifts?:string[] };
export type Doctor = { id:string; name:string; email:string; passwordHash:string; role:"doctor"; nurses:Nurse[] };
export type NurseUser = {id:string;name:string;email:string;passwordHash:string;role:"nurse";nurseId:string;mustChangePassword?:boolean};
export type AdminUser = {id:string;name:string;email:string;passwordHash:string;role:"admin"};
export type User = Doctor | NurseUser | AdminUser;
export type FloorRecord = { id:FloorId; name:string; description:string; beds:number };
export type Patient = { id:string; fullName:string; birthDate:string; reason:string; allergies:string; emergencyContact:string; emergencyPhone:string; floor:FloorId|"unassigned"; bed?:number; admittedAt:string; dischargedAt?:string; dischargeReason?:string; status:"admitted"|"discharge_pending"|"discharged"|"transferred"; notes:string };
export type Shift = { id:string; nurseId:string; floor:FloorId; date:string; kind:"day"|"night"; startsAt:string; endsAt:string; status:"scheduled"|"cancelled" };
export type MedicationOrder = { id:string; patientId:string; name:string; dose?:string; times:string[]; frequency?:string; startDate:string; endDate?:string; floor:FloorId; nurseId?:string; status:"active"|"cancelled"; notes?:string };
export type CareTask = { id:string; patientId:string; medicationId?:string; title:string; scheduledAt:string; nurseId?:string; floor:FloorId; status:"pending"|"in_progress"|"completed"|"skipped"|"transferred"; notes?:string; vitalId?:string };
export type VitalRecord = { id:string; taskId:string; patientId:string; temperature?:string; bloodPressure?:string; heartRate?:string; respiratoryRate?:string; oxygenSaturation?:string; notes?:string; recordedBy:string; recordedAt:string };
export type AuditEvent = { id:string; actorId:string; actorRole:string; action:string; entity:string; entityId?:string; details?:Record<string,unknown>; at:string };
export type Store = {schemaVersion:2|3;revision:number;users:User[];floors:FloorRecord[];patients:Patient[];shifts:Shift[];medications:MedicationOrder[];tasks:CareTask[];vitals:VitalRecord[];audit:AuditEvent[];chatHistory:Record<string,string[]>;settings?:{agyEnabled:boolean}};
const Intent = z.enum(["create_nurse","move_nurse","update_floor","create_patient","assign_patient","move_patient","discharge_patient","create_shift","check_availability","create_medication","create_task","complete_task","query_floor","query_patient"]);
const Operation = z.object({action:z.string().optional(),nurseId:z.string().optional(),patientId:z.string().optional(),taskId:z.string().optional(),from:Floor.optional(),to:Floor.optional(),floor:Floor.optional(),name:z.string().optional(),title:z.string().optional(),scheduledAt:z.string().optional(),fullName:z.string().optional(),birthDate:z.string().optional(),alias:z.string().optional(),email:z.string().email().optional(),phone:z.string().optional(),bed:z.number().int().positive().optional(),reason:z.string().optional(),allergies:z.string().optional(),emergencyContact:z.string().optional(),emergencyPhone:z.string().optional(),notes:z.string().optional(),date:z.string().optional(),kind:z.enum(["day","night"]).optional(),dose:z.string().optional(),times:z.array(z.string()).optional(),frequency:z.union([z.string(),z.number()]).optional(),startDate:z.string().optional(),endDate:z.string().optional(),status:z.string().optional()});
export const Plan = z.object({type:z.enum(["proposal","clarification","rejected","no_change"]),message:z.string().max(1000),intent:Intent.optional(),missing:z.array(z.string()).optional(),operations:z.array(Operation).max(20)});
export type Plan = {type:"proposal"|"clarification"|"rejected"|"no_change";message:string;intent?:string;missing?:string[];operations:any[]};
